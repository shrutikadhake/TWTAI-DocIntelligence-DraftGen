{
  "skill_name": "docs-as-code-generator",
  "evals": [
    {
      "id": 1,
      "eval_name": "auto-detect-all-applicable-topics",
      "prompt": "Convert `resources/v10_12_rate_limit_chat.txt` into HTML documentation. Do not tell me which topic type to use — analyze the chat, decide which documentation topic types it can support, and generate each applicable one. Validate all content against Microsoft Manual of Style using the Microsoft Learn MCP server. Flag any missing procedural elements with HTML comments.",
      "expected_output": "The skill writes two separate HTML files to docs-as-code-generator-workspace/output/v10_12_rate_limit_chat/ and returns a summary (not the full HTML). Summary states: (1) which topic types were detected (task and reference); (2) the file paths of generated files (task_configure-custom-api-rate-limits.html and reference_api-rate-limit-configuration.html); (3) note that concept topic was not generated. Each file is semantic HTML5 (no <html>/<head>/<body> wrappers) with: Task topic — <h1> title, intro, Prerequisites with /etc/scanner/config.yml and custom_limits_enabled in <code>, <ol> of 5+ steps, Expected Results (429 response), Troubleshooting (403 error). Reference topic — configuration file, schema with YAML, CLI syntax, parameters, status codes. Every element grounded in source. No HTML displayed in chat — only file summary.",
      "files": []
    },
    {
      "id": 2,
      "eval_name": "explicit-type-restriction-task-only",
      "prompt": "From `resources/v10_12_rate_limit_chat.txt`, I only want a Task topic — do not generate concept or reference topics. Extract the procedural steps for configuring custom API rate limits, including prerequisites, expected results, and troubleshooting. Apply Microsoft Manual of Style validation.",
      "expected_output": "The skill writes a single HTML file to docs-as-code-generator-workspace/output/v10_12_rate_limit_chat/task_configure-custom-api-rate-limits.html because the user explicitly restricted to task type. Returns a summary showing: (1) detected type: task only; (2) single file path written; (3) no mention of reference/concept topics. The file contains semantic HTML5 Task topic: <h1> title, intro, Prerequisites with /etc/scanner/config.yml and YAML edit in <code>, <ol> with 5+ numbered steps, Expected Results (429), Troubleshooting (403 when prerequisite missed). No <html>/<head>/<body> wrappers. No HTML shown in chat — only file summary.",
      "files": []
    },
    {
      "id": 3,
      "eval_name": "subset-detection-with-gap-flagging",
      "prompt": "Process `resources/v10_12_rate_limit_chat.txt` and produce documentation. Let the skill decide the topic type(s). If the source only weakly supports a given type, still surface that decision and flag what conceptual or reference detail is missing rather than inventing it. Validate against Microsoft Manual of Style.",
      "expected_output": "The skill writes two HTML files (task and reference) to docs-as-code-generator-workspace/output/v10_12_rate_limit_chat/ and returns a summary. Summary states which types were detected (task, reference) and which were skipped with reason (concept fails threshold test). Lists file paths created. Within each HTML file, missing reference details (e.g., types/ranges for api-limit, timeout semantics) are flagged with <!-- MISSING_ELEMENT: ... --> HTML comments rather than invented. No concept file written. No HTML displayed in chat — only summary of detected types, file paths, and flagged gaps."
      "files": []
    }
  ]
}
